==用途:如果集裁生產完的軸需要建帳的位置 (FROM跟TO的位置)==

==步驟1. 如果是在3樓的軸要入庫,在儲區位置選擇T 3F車間倉==
==步驟2. 在儲格代碼輸入SY (虛擬倉)==
==步驟3. 按下查詢,會出現很多空儲位的選項==
==步驟4. 選擇任一空儲位後,輸入要入的軸ID==
==步驟5. 按下修改==

==完成後可以到[[OC361包裝作業(押出生產後)]]繼續作業==

![[Pasted image 20250911101445.png]]